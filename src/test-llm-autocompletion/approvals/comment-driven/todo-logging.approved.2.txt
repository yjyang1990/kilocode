function processOrder(order) {
  console.log(`Processing order: ${JSON.stringify(order)}`);
  console.log(`Order validation started at: ${new Date().toISOString()}`);
  const result = validateOrder(order);
  console.log(`Order validation completed at: ${new Date().toISOString()}`);
  console.log(`Order processing result: ${JSON.stringify(result)}`);
  return result;
}